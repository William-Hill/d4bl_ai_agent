# Core Dependencies
# FastAPI and server
fastapi>=0.112.0
uvicorn[standard]>=0.30.0

# CrewAI and AI Agent Framework
crewai[tools,google-genai]==1.5.0
google-generativeai>=0.8.0  # Google AI embeddings for CrewAI memory

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Caching
cachetools>=5.3.0

# Scheduling
APScheduler>=3.10,<4

# Tools and utilities
python-dotenv>=1.1.1
litellm>=1.0.0
ollama>=0.1.0  # Required for Ollama embeddings in CrewAI memory
pypdf>=4.0  # PDF extraction in content extractor
trafilatura>=1.6  # HTML content extraction
httpx>=0.27  # HTTP client for ingestion scripts and search tools

# Langfuse for observability and tracing
langfuse>=2.0.0
# OpenInference instrumentation for CrewAI and LiteLLM
openinference-instrumentation-crewai>=0.1.0
# CrewAI >= 0.63.0 uses LiteLLM, so we need this for LLM call visibility
openinference-instrumentation-litellm>=0.1.0
# Additional dependencies for evaluations
pandas>=2.0.0  # Required for running evaluations

# Archived Phoenix evals

These files were used for Phoenix-based evaluations (hallucination, bias, reference) and trace ingestion/logging. The project now uses Langfuse-based evaluators. Keep these for historical reference; they are not used in the active app.
